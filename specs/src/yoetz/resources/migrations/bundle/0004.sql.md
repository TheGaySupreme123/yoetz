# src/yoetz/resources/migrations/bundle/0004.sql — installed byte-identical migration mirror

**Wave:** D | **ADRs:** ADR-003, ADR-007 | **Imports (spec-tree):**
`specs/migrations/bundle/0004.sql.md`, `specs/src/yoetz/resources/manifest.json.md` |
**Imported by:** packaging and runtime migration loaders

## Purpose

Installed package mirror of `migrations/bundle/0004.sql`. Bytes must match the public source
exactly after `scripts/verify_resource_manifest.py --sync`.

## Public surface

Logical resource: `migrations/bundle/0004.sql`. Installed path:
`src/yoetz/resources/migrations/bundle/0004.sql`.

## Behavior

Runtime loads this SQL only after resource-manifest verification. No regeneration at runtime.

## Errors and edge cases

Manifest digest or size mismatch fails packaging/startup gates.

## Invariants

1. Installed bytes equal public source bytes.
2. Hand-editing either copy without sync is forbidden.

## Tests

Resource parity and packaging suites.

## Open questions

None.
