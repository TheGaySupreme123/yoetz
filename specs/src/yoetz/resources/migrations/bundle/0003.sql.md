# src/yoetz/resources/migrations/bundle/0003.sql — installed bundle migration 0003

**Wave:** D/F | **ADRs:** ADR-009, ADR-010 | **Imports (spec-tree):**
`specs/migrations/bundle/0003.sql.md`, `specs/src/yoetz/resources/manifest.json.md` |
**Imported by:** installed migration registry and packaging verification

## Purpose

Install the byte-identical canonical migration `migrations/bundle/0003.sql` in the wheel.

## Public surface

Logical resource `migrations/bundle/0003.sql`, bound by resource manifest size and SHA-256.

## Behavior

The owning resource synchronization script performs an opaque byte copy and updates manifests.

## Errors and edge cases

Missing files or byte/digest/size drift fail resource verification.

## Invariants

1. Canonical and installed bytes are identical.
2. Table semantics are owned only by the canonical migration spec.

## Tests

`scripts/verify_resource_manifest.py` and installed-wheel migration tests.

## Open questions

None.
